---
summary: "CLI reference for `special-agent uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `special-agent uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
special-agent uninstall
special-agent uninstall --all --yes
special-agent uninstall --dry-run
```
